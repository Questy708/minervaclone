{
  "entities": {
    "Classroom": {
      "title": "Classroom",
      "description": "Represents the active live state of the seminar classroom.",
      "type": "object",
      "properties": {
        "activeActivityId": { "type": "string", "description": "The active lesson activity ID" },
        "currentSpeakerId": { "type": "string", "description": "ID of current talking student" },
        "subSpeakerId": { "type": "string", "description": "ID of currently sub speaking student" },
        "debateText": { "type": "string", "description": "Active spoken speech text or discussion prompt" },
        "feedback": { "type": "object", "description": "Interactive reaction emoji counts" },
        "feedbackGauges": { "type": "object", "description": "Live yes/no poll counter state" },
        "isPlaying": { "type": "boolean", "description": "Whether the seminar clock is active and counting" },
        "elapsedSeconds": { "type": "integer", "description": "Active duration elapsed" }
      },
      "required": ["activeActivityId", "debateText", "feedback", "feedbackGauges"]
    },
    "Student": {
      "title": "Student",
      "description": "Represents a student or instructor in the active session.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "isSpeaking": { "type": "boolean" },
        "active": { "type": "boolean" },
        "avatarColor": { "type": "string" },
        "unreadCount": { "type": "integer" }
      },
      "required": ["id", "name", "isSpeaking", "active", "avatarColor"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "A dynamic in-class chat room message.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "sender": { "type": "string" },
        "text": { "type": "string" },
        "timestamp": { "type": "string" },
        "isInstructor": { "type": "boolean" }
      },
      "required": ["id", "sender", "text", "timestamp"]
    },
    "Course": {
      "title": "Course",
      "description": "An academic curriculum with dynamic lessons generated by Course Builder.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "courseCode": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "recommendedHCs": { "type": "array" },
        "lessonTimeline": { "type": "array" }
      },
      "required": ["id", "courseCode", "title", "description"]
    }
  },
  "firestore": {
    "/classroom/{classroomId}": {
      "schema": "Classroom",
      "description": "Holds the live state of a classroom arena."
    },
    "/classroom/{classroomId}/students/{studentId}": {
      "schema": "Student",
      "description": "Dynamic students roster in a live session."
    },
    "/classroom/{classroomId}/chat/{messageId}": {
      "schema": "ChatMessage",
      "description": "Live chat thread of a session."
    },
    "/courses/{courseId}": {
      "schema": "Course",
      "description": "Curriculum index details owned by instructors."
    }
  }
}
